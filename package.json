{
  "name": "kabar-monorepo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --names \"CLIENT,BACKEND,WEBSOCKET\" \"npm run dev --prefix client\" \"npm run dev --prefix backend\" \"npm run dev --prefix websocket-example\"",
    "install:all": "cd backend && npm ci && cd ../client && npm ci",
    "build": "npm run build:backend && npm run build:client",
    "build:backend": "cd backend && npm run build",
    "build:client": "cd client && npm run build",
    "lint": "concurrently --names \"BACKEND,CLIENT\" \"npm run lint:backend\" \"npm run lint:client\"",
    "lint:backend": "cd backend && npm run lint",
    "lint:client": "cd client && npm run lint",
    "typecheck": "npm run typecheck:backend",
    "typecheck:backend": "cd backend && npx tsc --noEmit",
    "check": "npm run typecheck && npm run lint && npm run build",
    "test": "concurrently --names \"CLIENT,BACKEND\" \"npm run test:client\" \"npm run test:backend\"",
    "test:backend": "cd backend && npm test",
    "test:client": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}